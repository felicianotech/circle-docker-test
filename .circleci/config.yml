version: 2.1
workflows:
  main:
    jobs:
      - build
jobs:
  build:
    docker:
      - image: alpine:3.7
    steps:
      - checkout
      - run: ls -lah /bin/sh
      - run: apk add bash
      - run: rm /bin/sh && ln -s /bin/bash /bin/sh
      - run: ls -lah /bin/sh
      - run: echo "CHICKEN=you" >> $BASH_ENV
      - run: echo $CHICKEN
