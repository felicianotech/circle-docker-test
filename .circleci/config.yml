version: 2.1

workflows:
  main:
    jobs:
      - build

jobs:
  build:
    machine:
      image: ubuntu-2004:canary
    steps:
      - checkout
      - run: |
          sudo snap install sonar
      - run: |
          echo $PATH
          snap run sonar version --short
          sonar version
