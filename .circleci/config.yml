version: 2.1

workflows:
  main:
    jobs:
      - build

jobs:
  build:
    machine:
      image: ubuntu-2004:canary
    steps:
      - checkout
      - run: |
          sudo snap install sonar
      - run: |
          echo $PATH
          echo "============================"
          cat ~/.bashrc
          echo "============================"
          snap run sonar version
          sonar version
