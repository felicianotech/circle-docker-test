version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8.6
    steps:
      - checkout
      - run: |
          node --version
          npm --version
          sudo npm update -g
          sudo npm install -g
          sudo npm install npmlog -g
          sudo npm install npm@latest -g
          npm --version
