version: 2.1

workflows:
  main:
    jobs:
      - build

jobs:
  build:
    machine:
      image: ubuntu-2004:202008-01
    steps:
      - checkout
      - run: |
          echo "PATH=$PATH:/snap/bin" >> $BASH_ENV
          sudo snap install gotham
          gotham version
