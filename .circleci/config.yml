version: 2
jobs:
  test:
    docker:
      - image: circleci/golang:1.8
    working_directory: ~/project
    steps:
      - checkout: {}
      - run:
          name: "Check Docker"
          command: echo "Hi"
      - run:
          name: "Check Docker"
          command: echo "Hey"
