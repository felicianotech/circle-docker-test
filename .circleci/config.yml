version: 2.1

orbs:
  n: circleci/node@dev:d62e948

workflows:
  main:
    jobs:
      - build

jobs:
  build:
    machine:
      image: ubuntu-2004:202101-01
    steps:
      - run: gcloud version
      - run: node --version
      - n/install
      - run: node --version
