version: 2.1

workflows:
  main:
    jobs:
      - build

jobs:
  build:
    machine:
      image: ubuntu-2004:202104-01
      #image: ubuntu-1604:202104-01
    steps:
      - checkout
      - run: |
          echo $JAVA_HOME
          which javac
          which java
          aws --version
          yq --version
          go version
          jq --version
          docker --version
          node --version
          java -version
          cat /etc/os-release
          sudo apt-get update
          echo "================================================="
          ls -lah /usr/local | grep -i "go"
          echo "================================================="
          ls -lah /usr/local/go
