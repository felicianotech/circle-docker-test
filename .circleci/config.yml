version: 2.1
workflows:
  main:
    jobs:
      - build
jobs:
  build:
    docker:
      - image: felicianotech/test:1
    steps:
      - checkout
      - run:
          name: "Do all the things!"
          command: |
            ps aux | grep -i Xvfb
            echo "====================================="
            echo $DISPLAY
