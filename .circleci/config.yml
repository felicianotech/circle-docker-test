version: 2
jobs:
  build:
    docker:
      - image: ubuntu:16.04
    working_directory: ~/project
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: "Check Docker"
          command: sleep 20; docker --version
