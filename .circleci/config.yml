version: 2.1
workflows:
  main:
    jobs:
      - build
jobs:
  build:
    docker:
      - image: cimg/base:2020.12
    steps:
      - checkout
      - run: echo "CHICKEN=you" >> $BASH_ENV
      - run: echo $CHICKEN
