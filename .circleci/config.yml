version: 2.1
workflows:
  main:
    jobs:
      - build
jobs:
  build:
    docker:
      - image: cimg/node:16.0
    steps:
      - checkout
      - run:
          name: "Do all the things!"
          command: |
            echo $SHELL
            echo $BASH_ENV
            yarn global add electron
            electron --version
