version: 2.0
workflows:
  version: 2
  main:
    jobs:
      - build
jobs:
  build:
    docker:
      - image: circleci/golang:1.13
      - image: circleci/postgres:10
    steps:
      - checkout
      - run:
          name: "Let's test things!!"
          command: |
            docker --version
      - run:
          name: "Let's break things!!"
          command: |
            docker version
