version: 2.1

orbs:
    gcp: circleci/gcp-cli@1.0.2

workflows:
  main:
    jobs:
      - build

jobs:
  build:
    docker:
      - image: circleci/golang:1.12
    #machine: true
    steps:
      - gcp/install
      - run: gcloud version
