version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8.6
    steps:
      - checkout
      - run: |
          node --version
          npm --version
          sudo npm --version
          #npm update
          #sudo npm update -g
          npm install npm@latest
          npm --version
