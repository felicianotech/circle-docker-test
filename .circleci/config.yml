version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8.6
    steps:
      - checkout
      - run: |
          node --version
          npm --version
          npm update
          npm update -g
          sudo npm install npm -g
          npm --version
