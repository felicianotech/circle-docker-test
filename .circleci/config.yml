version: 2.1
workflows:
  main:
    jobs:
      - build
jobs:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: "Do all the things!!"
          command: |
            sudo addgroup bumpty
            sudo adduser circleci bumpty
            groups
      - run:
          name: "Do all the things!!"
          command: |
            groups
