version: 2.1

orbs:
    python: circleci/python@0.1.8

workflows:
  main:
    jobs:
      - build

jobs:
  build:
    executor: python/default
    steps:
      - run: |
          python --version
          pip --version
      - run: echo "Django==1.3" > requirements.txt
      - python/save-cache
