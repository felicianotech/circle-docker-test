version: 2.1

orbs:
    python: circleci/python@0.1.5

workflows:
  main:
    jobs:
      - build

jobs:
  build:
    docker:
      - image: circleci/golang:1.12
    #machine: true
    steps:
      - python/install:
          version: 3.7.4
      - run: |
          python --version
          pip --version
