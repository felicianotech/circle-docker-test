version: 2.1

orbs:
  theorb: circleci/rememborb@0.0.1

workflows:
  main:
    jobs:
      - build
      - build2:
          requires:
            - build

jobs:
  build:
    docker:
      - image: ubuntu:18.04
    steps:
      - run: apt-get update && apt-get install -y git tar
      - theorb/remember:
          env_var: TEST_VAR
          value: 'HappyBirthday'
      - run: |
          echo $TEST_VAR
  build2:
    docker:
      - image: ubuntu:18.04
    steps:
      - run: apt-get update && apt-get install -y git tar
      - theorb/recall:
          env_var: TEST_VAR
      - run: |
          echo ${TEST_VAR}
