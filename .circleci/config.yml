version: 2.1
workflows:
  main:
    jobs:
      - build
jobs:
  build:
    docker:
      - image: cimg/android:priceline
    steps:
      - checkout
      - run:
          name: "Do all the things!"
          command: |
            gcloud --version
