version: 2.0
jobs:
  build:
    docker:
      - image: circleci/golang:1.11
      - image: circleci/postgres:10
    steps:
      - checkout
      - run:
          name: Debug
          command: |
            sleep 30
            ps aux
            sudo apt update && sudo apt install net-tools
            sudo netstat -plntu
