version: 2
jobs:
  build:
    docker:
      - image: circleci/ruby:2.6.5
    steps:
      - checkout
      - run:
          name: Do Anything Bundler Related
          command: |
            bundler -v
            gem install bundler
            bundler -v
