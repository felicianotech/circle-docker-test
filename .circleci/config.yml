version: 2.1

orbs:
  theorb: circleci/rememborb@0.0.1

workflows:
  main:
    jobs:
      - build
      - build2:
          requires:
            - build

jobs:
  build:
    docker:
      - image: cimg/base:2019.11
    steps:
      - theorb/remember:
          env_var: TEST_VAR
          value: 'HappyBirthday'
      - run: |
          echo $TEST_VAR
  build2:
    docker:
      - image: cimg/base:2019.11
    steps:
      - theorb/recall:
          env_var: TEST_VAR
      - run: |
          echo $TEST_VAR
