version: 2.1
workflows:
  main:
    jobs:
      - build
jobs:
  build:
    docker:
      - image: alpine:3.7
    steps:
      - checkout
      - run: echo "CHICKEN=you" >> $BASH_ENV
      - run: echo $CHICKEN
