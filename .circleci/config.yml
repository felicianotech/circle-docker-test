version: 2
jobs:
  build:
    docker:
      - image: circleci/golang:1.10
    steps:
      - checkout
      - run:
          name: "Check Envar"
          command: |
            echo $CIRCLE_WORKING_DIRECTORY
            CIRCLE_WORKING_DIRECTORY="${CIRCLE_WORKING_DIRECTORY/#\~/$HOME}"
            echo $CIRCLE_WORKING_DIRECTORY
            mkdir -p $CIRCLE_WORKING_DIRECTORY/foo/bar
            ls -lah
