version: 2.1

orbs:
  cli: circleci/circleci-cli@0.1

workflows:
  main:
    jobs:
      - build

jobs:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - cli/install
      - run:
          name: "Check version"
          command: circleci version
