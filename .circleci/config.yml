version: 2.1
workflows:
  main:
    jobs:
      - build
jobs:
  build:
    docker:
      - image: cimg/node:12.19.0-browsers
    steps:
      - checkout
      - run:
          name: "Do all the things!"
          command: |
            echo $SHELL
