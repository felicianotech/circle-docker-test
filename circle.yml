machine:
  environment:
    CRAZZY: hi
dependencies:
  override:
    - ls -lah ~/
  post:
    - echo "Hi"
    - host registry.npmjs.com
    - dig A registry.npmjs.com

test:
  pre:
    - ls -lah ~/
    - ps aux | grep mongo
    - go version
    - echo "manual" | sudo tee /etc/init/mongod
    - ps aux | grep mongo
    - netstat -plntu
  override:
    - git --version
    - nproc --all
  post:
    - ps aux
