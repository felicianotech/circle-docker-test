machine:
  environment:
    CRAZZY: hi
  node:
    version: 8.0.0

dependencies:
  override:
    - echo "Hi"
  post:
    - npm --version
    - node --version

test:
  pre:
    - ls -lah ~/
  override:
    - "true"
  post:
    - ps aux
    - heroku --version
    - heroku --help
    - heroku update

deployment:
  prod:
    branch: master
    commands:
      - echo "Hi"
      - echo $MY_AWESOME_VAR
#      - echo "============"
