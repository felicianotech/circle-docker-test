machine:
  environment:
    CRAZZY: hi

dependencies:
  override:
    - echo "Hi"
  post:
    - echo "Bye!"

test:
  pre:
    - ls -lah ~/
  override:
    - "true"
  post:
    - heroku --version
    - heroku update
    - heroku --version
    - wget -qO- https://cli-assets.heroku.com/install-ubuntu.sh | sh
    - heroku --version

deployment:
  prod:
    branch: master
    commands:
      - echo "Hi"
      - echo $MY_AWESOME_VAR
