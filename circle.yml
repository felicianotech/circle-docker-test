machine:
  environment:
    CRAZZY: hi
  services:

dependencies:
  override:
    - echo "Hi!"
  post:
    - echo "Bye!"

test:
  pre:
    - ls -lah
  override:
    - "true"
  post:
    - |
        if [[ ! $CIRCLE_JOB ]]; then                                                                                                                              
          echo "CIRCLE_JOB is not set"                                                                                                                               
        else                                                                                                                                                         
          echo "CIRCLE_JOB is set"                                                                                                                                   
          echo "And the value is: " $CIRCLE_JOB                                                                                                                      
        fi
