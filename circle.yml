machine:
  environment:
    CRAZZY: hi
  node:
    version: 8.0.0

dependencies:
  override:
    - echo "Hi"
  post:
    - npm --version
    - nodejs --version

test:
  pre:
    - ls -lah ~/
  override:
    - git --version
  post:
    - ps aux

deployment:
  prod:
    branch: master
    commands:
      - echo "Hi"
      - echo $MY_AWESOME_VAR
      - echo "============"
