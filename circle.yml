general:
  artifacts:
    - /var/log/upstart/docker.log
machine:
  pre:
    - export MY_EVAR=`cat circle-docker-test/test.txt`
  services:
    - docker
  post:
    - sudo chmod +r /var/log/upstart/docker.log
test:
  override:
    - echo $MY_EVAR
    - docker run ubuntu /bin/echo 'Hello world'
