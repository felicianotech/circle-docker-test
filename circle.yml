machine:
  environment:
    CRAZZY: hi
  services:
    - docker

dependencies:
  override:
    - echo "Hi"
    - echo $CRAZZY
    - echo $crazzy
    - echo $Happy
    - echo $HAPPY
    - docker pull promact/aspnetcore-node
  post:
    - echo "Bye!"

test:
  pre:
    - ls -lah ~/
  override:
    - "true"
  post:
    - heroku --version

deployment:
  prod:
    branch: master
    commands:
      - echo "Hi"
      - echo $MY_AWESOME_VAR
