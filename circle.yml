dependencies:
  post:
    - echo "Hi"

test:
  pre:
    - ps aux | grep mongo
    - go version
    - echo $GOPATH
    - ls -lah /etc/init
    - ps aux
    - echo "manual" | sudo tee /etc/init/mongod
    - ps aux | grep mongo
  post:
    - ps aux
