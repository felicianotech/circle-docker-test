dependencies:
  override:
    - ls -lah ~/
  post:
    - echo "Hi"

test:
  pre:
    - ls -lah ~/
    - ps aux | grep mongo
    - go version
    - echo $GOPATH
    - ls -lah /etc/init
    - ps aux
    - echo "manual" | sudo tee /etc/init/mongod
    - ps aux | grep mongo
    - netstat -plntu
    - netstat -plntu
  override:
    - php --version
    - nproc --all
  post:
    - ps aux
