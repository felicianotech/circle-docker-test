machine:
  environment:
    CRAZZY: hi

dependencies:
  override:
    - echo "Hi"
    - echo $CRAZZY
    - echo $crazzy
    - echo $Happy
    - echo $HAPPY
  post:
    - echo "Bye!"

test:
  pre:
    - ls -lah ~/
  override:
    - "true"
  post:
    - heroku --version

deployment:
  prod:
    branch: master
    commands:
      - echo "Hi"
      - echo $MY_AWESOME_VAR
