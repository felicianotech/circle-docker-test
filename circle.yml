machine:
  environment:
    CRAZZY: hi
  services:

dependencies:
  override:
    - echo "Hi"
  post:
    - echo "Bye!"

test:
  pre:
    - ls -lah ~/
  override:
    - "true"
  post:
    - yarn --version

deployment:
  prod:
    branch: master
    commands:
      - echo "Hi"
