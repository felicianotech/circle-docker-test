dependencies:
  override:
    - ls -lah ~/
  post:
    - echo "Hi"

test:
  pre:
    - ls -lah ~/
    - ps aux | grep mongo
    - go version
    - echo "manual" | sudo tee /etc/init/mongod
    - ps aux | grep mongo
    - netstat -plntu
  override:
    - git --version
    - nproc --all
  post:
    - ps aux
