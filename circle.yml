general:
  artifacts:
  #  - /var/log/upstart/docker.log
machine:
  services:
  #  - docker
  #  - cassandra
  environment:
    #MY_EVAR: cat circle-docker-test/test.txt
  #php:
  #  version: 7.0.3
  post:
  #  - sudo chmod +r /var/log/upstart/docker.log
dependencies:
  pre:
    # Check which image we're using
    - lsb_release -a
  # - sudo rm /etc/apt/sources.list.d/google*
  # - sudo apt-get update
test:
  override:
  #  - echo $MY_EVAR
    - echo $TEST_VAR
    - echo $PATH
    - sudo bash -c 'echo CRAAZZY > /home/ubuntu/test.txt'
    #- docker run ubuntu /bin/echo 'Hello world'
    #- php test.php
    #- kubectl --version
notify:
  webhooks:
    #- url: https://feliciano.tech/postmaster.php?testing=1&testing2=$CIRCLE_BUILD_NUM
    #- url: https://feliciano.tech/${TEST_VAR}master.php
