machine:
  environment:
    CRAZZY: hi

dependencies:
  override:
    - yarn --version
  post:
    - echo "Hi"
    - apt-cache depends circleci-php-7.1.3
    - apt-cache depends mysql-community-server
    - apt-cache depends mysql-community-client
    - apt-cache depends mysql-common

test:
  pre:
    - ls -lah ~/
  override:
    - git --version
  post:
    - ps aux

deployment:
  prod:
    branch: master
    commands:
      - echo "Hi"
      - echo $MY_AWESOME_VAR
      - echo "============"
