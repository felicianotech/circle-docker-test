machine:
  environment:
    CRAZZY: hi
  node:
    version: 8.0.0

dependencies:
  override:
    - echo "Hi"
  post:
    - npm --version
    - node --version

test:
  pre:
    - ls -lah ~/
  override:
    - "true"
  post:
    - ps aux
    - heroku --version
    - heroku --help
      #- heroku update
    - wget -qO- https://cli-assets.heroku.com/install-ubuntu.sh | sh
    - heroku --version

deployment:
  prod:
    branch: master
    commands:
      - echo "Hi"
      - echo $MY_AWESOME_VAR
#      - echo "============"
