dependencies:
  post:
    - echo "Hi"

test:
  pre:
    - ps aux | grep mongo
    - echo $PATH
    - go version
    - echo $GOPATH
    - ls -lah /etc/init
    - ps aux
    - echo "manual" | sudo tee /etc/init/mongod
    - ps aux | grep mongo
    - ls -lah /home
  post:
    - ps aux
