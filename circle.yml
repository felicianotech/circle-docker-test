machine:
  environment:
    CRAZZY: hi

dependencies:
  override:
    - yarn --version
  post:
    - echo "Hi"
    - apt-cache depends ampache-common
    - apt-cache rdepends ampache-common
    - apt-cache depends ampache
    - apt-cache rdepends ampache
    #- sudo apt-get remove mysql-server mysql-client mysql-common
    - sudo apt-get remove vim

test:
  pre:
    - ls -lah ~/
  override:
    - git --version
  post:
    - ps aux

deployment:
  prod:
    branch: master
    commands:
      - echo "Hi"
      - echo $MY_AWESOME_VAR
      - echo "============"
