machine:
  environment:
    CRAZZY: hi
  services:

dependencies:
  override:
    - echo "Hi!"
  post:
    - echo "Bye!"

test:
  pre:
    - ls -lah
  override:
    - "true"
  post:
    - |
        apache2 --version
        sudo apt-get update && sudo apt-get -y install apache2
        apache2 --version
