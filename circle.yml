general:
  artifacts:
    - /var/log/upstart/docker.log
machine:
  services:
    - docker
  post:
    - sudo chmod +r /var/log/upstart/docker.log
    - sudo export MY_EVAR=`circle-docker-test/test.txt`
test:
  override:
    - echo $MY_EVAR
    - docker run ubuntu /bin/echo 'Hello world'
