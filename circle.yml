general:
  artifacts: /var/log/upstart/docker.log
machine:
  services:
    - docker
  post:
    - sudo chmod +r /var/log/upstart/docker.log
test:
  override:
    - docker run ubuntu /bin/echo 'Hello world'
