machine:
  environment:
    CRAZZY: hi

dependencies:
  override:
    - yarn --version
  post:
    - echo "Hi"
    - sudo apt-get remove mysql-server mysql-client

test:
  pre:
    - ls -lah ~/
  override:
    - git --version
  post:
    - ps aux

deployment:
  prod:
    branch: master
    commands:
      - echo "Hi"
      - echo $MY_AWESOME_VAR
      - echo "============"
